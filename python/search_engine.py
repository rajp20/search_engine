class SearchEngine:
    def __init__(self, path):
        self.data = self.load_data(path)

    def load_data(path):

        return 0

    @staticmethod
    def search(query):
        print("Received query:", query)
        json_response = {
            "result": "hello world"
        }
        return json_response
